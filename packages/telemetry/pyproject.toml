[project]
name = "pi-telemetry"
version = "0.53.0"
description = "Vendor-neutral telemetry contracts — Python mirror of @earendil-works/pi-telemetry"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pi_telemetry"]
