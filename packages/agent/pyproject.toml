[project]
name = "pi-agent"
version = "0.53.0"
description = "Agent loop and state management — Python mirror of @mariozechner/pi-agent-core"
requires-python = ">=3.11"
dependencies = [
    "pi-ai",
    "pydantic>=2.0.0",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
agentfuse = [
    "dhms-agentfuse==3.7.3",
]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-mock>=3.14.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pi_agent"]

[tool.uv.sources]
pi-ai = { workspace = true }
